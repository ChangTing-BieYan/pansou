name: 同步上游

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 添加上游仓库
        run: |
          git remote add upstream https://github.com/fish2018/pansou.git || true
          git fetch upstream main

      - name: 检查上游是否有新提交
        id: check
        run: |
          LOCAL=$(git rev-parse HEAD)
          UPSTREAM=$(git rev-parse FETCH_HEAD)
          echo "LOCAL=$LOCAL"
          echo "UPSTREAM=$UPSTREAM"
          if [ "$LOCAL" = "$UPSTREAM" ]; then
            echo "✅ 上游没有新提交，跳过同步"
            exit 0
          else
            echo "🔄 上游有新提交"
          fi

      - name: 合并上游（上游优先）
        run: |
          git checkout main
          git merge upstream/main --no-edit || true

      - name: 恢复本地修改（仅限 ARMv7 兼容 Go 文件）
        run: |
          echo "🔁 恢复保留的 Go 文件（ARMv7 兼容修改）..."
          conflict_files=$(git diff --name-only --diff-filter=U)
          if [ -n "$conflict_files" ]; then
            for f in $conflict_files; do
              if [[ "$f" == *.go ]]; then
                git checkout --ours "$f"
                git add "$f"
              fi
            done
            git merge --continue || true
          else
            echo "⚡ 没有冲突文件，跳过恢复"
          fi

      - name: 提交并推送
        run: |
          git add .
          git diff --cached --quiet || git commit -m "🔄 同步上游仓库（保留 ARMv7 兼容 Go 修改）"
          git push origin main
